window.BT_NOTES = {
  "0": "# Slide 01 — Titre\n**Tomas**\n\nBonjour. On va vous présenter BobTricks, un système de **locomotion procédurale 2D** en C++20.\n\nAvant d'ouvrir le code, je veux poser une question simple : comment est-ce qu'un personnage de jeu vidéo marche ?\n\n### Approche 1 : animation enregistrée (keyframes)\n\nLa réponse classique, c'est de capturer un vrai humain qui marche, stocker ses positions d'articulations image par image, et rejouer ça en boucle. C'est ce que font la majorité des jeux — c'est rapide à produire, ça a l'air bien.\n\nMais le problème, c'est que cette animation est **morte**. Elle ne sait pas que le personnage accélère. Elle ne sait pas qu'il y a une pente. Si le joueur s'arrête à mi-cycle, le pied peut flotter dans les airs. Pour chaque nouveau comportement — marche lente, marche rapide, terrain pentu, arrêt brusque — il faut enregistrer une nouvelle animation et écrire des transitions à la main.\n\n### Approche 2 : simulation physique complète\n\nÀ l'opposé, on pourrait simuler chaque muscle, chaque os, chaque tendon avec un solveur de dynamique inverse. C'est ce que font les robots bipèdes en robotique. Le résultat est bioméchaniquement exact.\n\nMais c'est **extrêmement complexe à stabiliser**. Un solveur de corps articulé complet (type MuJoCo, PhysX articulations) peut diverger numériquement si un paramètre est mal réglé. Ce n'est pas raisonnable sur 3 mois de projet.\n\n### Notre compromis : animation procédurale\n\nOn génère le mouvement **à la volée** depuis un modèle physique compact. Le centre de masse est simulé, tout le reste est reconstruit géométriquement. Pas d'animation à stocker. Le personnage réagit à sa vitesse réelle, au terrain, à un déséquilibre — automatiquement.\n\nLa base scientifique vient de deux articles :\n- **Hof (2008)** sur le XCoM et la stabilité de la marche humaine\n- **Kajita et al. (2001)** sur le pendule inversé linéaire pour la génération de marche bipède\n\nLa présentation va suivre le pipeline dans l'ordre : d'abord l'architecture, puis CM → bassin → IK → rendu, puis la stabilité, le terrain, et la validation.",
  "1": "# Slide 02 — Fonctionnalités\n**Meleik**\n\nCette slide montre ce que l'utilisateur voit. Je vais expliquer non seulement *quoi*, mais *comment* chaque feature est branchée au reste du système.\n\n### SDL2 — la bibliothèque centrale\n\nSDL2 gère la fenêtre, les événements clavier, et le rendu 2D. Toutes les opérations graphiques passent par un `SDL_Renderer*` transmis au `CharacterRenderer` et au `SceneRenderer`. Le build graphique inclut les flags `-lSDL2 -lSDL2_mixer`, le build headless n'en a aucun — c'est prouvé dans le Makefile.\n\n### SDL_mixer — audio réactif à la simulation\n\nLes sons de pas ne sont pas joués sur un timer. Ils sont déclenchés par des **événements produits par `SimulationCore`** :\n\n```cpp\nevents.left_touchdown  = !ctx.prev_contact_left  && ch.foot_left.on_ground;\nevents.right_touchdown = !ctx.prev_contact_right && ch.foot_right.on_ground;\nevents.landed_from_jump = ctx.prev_jump_flight_active && !ch.jump_flight_active\n                        && (events.left_touchdown || events.right_touchdown);\n```\n\n`EffectsSystem` consomme ces événements et joue le son correspondant via SDL_mixer. Résultat : les pas sont toujours synchronisés avec la physique réelle, pas avec une animation.\n\n### Dear ImGui — interface de debug en temps réel\n\nImGui est utilisé pour exposer les paramètres du `AppConfig` sous forme de sliders. On peut changer la hauteur nominale du CM, la rigidité de la jambe, le lean max — et voir l'effet immédiatement sans recompiler. C'est ce qui nous a permis de tuner les paramètres itérativement.\n\n### Les overlays — relier le visible à l'invisible\n\nLes overlays sont la vraie interface entre la simulation et l'observateur. Chaque overlay correspond à une grandeur calculée :\n- Point bleu = $x_{cm}$, $y_{cm}$ — l'état autoritatif\n- Losange = $\\xi = x_{cm} + v_x / \\omega_0$ — le XCoM\n- Cercles verts/rouges = pieds pinned / en swing\n- Ligne horizontale = intervalle de support $[x_L, x_R]$\n- Arc en pointillés = trajectoire parabolique du pied en swing\n\nPendant la démo, activer les overlays permet de voir **exactement** ce que le code calcule.",
  "2": "# Slide 03 — Architecture\n**Gedik**\n\nJe vais vous expliquer la décision de conception la plus importante du projet : pourquoi `SimulationCore` ne dépend pas de SDL.\n\n### Le problème qu'on a voulu éviter\n\nSi la physique dépendait du rendu, tester la marche impliquerait d'ouvrir une fenêtre, d'attendre que le personnage marche, d'observer visuellement. C'est lent, non reproductible, et impossible en intégration continue.\n\nOn a donc posé une règle dès le début : **`src/core/` ne peut pas inclure SDL**. Cette règle est vérifiable mécaniquement — si quelqu'un l'enfreignait, le build headless planterait immédiatement à la compilation.\n\n### Preuve par le Makefile\n\n```makefile\n# Build graphique : tout\nSRC_DIRS := src src/app src/config src/core/math src/core/character \\\n            src/core/locomotion src/core/runtime src/core/simulation \\\n            src/core/telemetry src/core/terrain src/render src/debug\n\n# Build headless : core seulement, sans SDL ni ImGui ni render\nHEADLESS_DIRS := src/config src/core/character src/core/locomotion \\\n                 src/core/simulation src/core/telemetry src/core/terrain \\\n                 src/headless\n```\n\nLes répertoires `src/render`, `src/app`, `src/debug` sont absents du build headless. C'est structurel, pas juste conventionnel.\n\n### Deux chemins d'exécution vers le même noyau\n\nL'application graphique SDL et le binaire headless partagent **exactement** le même `SimulationCore::step()`. Pas une version simplifiée — le même code, compilé différemment. Quand on corrige un bug en headless, il est aussi corrigé dans le graphique.\n\n### Structures de données principales\n\n- `CMState` : position, vitesse, accélération du CM\n- `CharacterState` : bassin, genoux, pieds, torse, tête, bras — la pose complète\n- `FootState` : position, état (pinned / swinging / airborne), cible de swing\n- `BalanceState` : $\\omega_0$, $\\xi$, MoS — les indicateurs de stabilité\n- `SimState` : agrège tout l'état de simulation\n- `ScenarioDef` + `TelemetryRow` : infrastructure des tests headless\n\nChaque structure a une responsabilité unique et est passée par valeur ou référence constante — pas de pointeurs nus, pas d'allocation manuelle.",
  "3": "# Slide 04 — Le CM est la vérité\n**Tomas**\n\nJe vais vous expliquer la décision physique centrale du projet.\n\n### Pourquoi simuler uniquement le centre de masse ?\n\nLe centre de masse est la **variable physique minimale** qui suffit pour modéliser la dynamique de marche d'un bipède. C'est un résultat théorique, pas une simplification arbitraire. Le modèle du pendule inversé linéaire (Kajita et al., 2001) montre qu'un CM à hauteur approximativement constante se comporte comme un pendule inversé — et c'est ce modèle qui justifie le XCoM de Hof.\n\nEn pratique, ça veut dire : on ne simule pas les genoux, les chevilles, les hanches. On simule un point de masse, et tout le reste est **reconstruit géométriquement** à partir de lui. Ça réduit drastiquement la complexité numerique, l'état à stocker, et la difficulté des tests.\n\n### Intégration du CM — pourquoi Euler semi-implicite ?\n\nIl y a plusieurs façons d'intégrer $\\ddot{x} = a$ :\n\n- **Euler explicite** : $v_{n+1} = v_n + a \\cdot dt$, puis $x_{n+1} = x_n + v_n \\cdot dt$ → instable pour les systèmes oscillatoires\n- **Euler semi-implicite** : $v_{n+1} = v_n + a \\cdot dt$, puis $x_{n+1} = x_n + v_{n+1} \\cdot dt$ → stable, conserve l'énergie à long terme\n- **Runge-Kutta 4** : précis mais 4× plus coûteux, et le pas fixe nous suffit\n\nOn utilise le semi-implicite parce qu'il est stable pour nos oscillations et que le pas de temps fixe le rend **déterministe** — essentiel pour que les tests headless soient reproductibles.\n\n### Contrôle vertical — attracteur tanh, pas un ressort\n\nOn ne tire pas le CM vers sa hauteur cible avec un ressort de Hooke ($F = k \\cdot \\Delta y$). Un ressort oscille. À la place, on utilise un attracteur non-linéaire :\n\n$$v_{y,\\text{want}} = v_{y,\\text{max}} \\cdot \\tanh\\!\\left(\\frac{y_{\\text{tgt}} - y}{d_{\\text{soft}}}\\right)$$\n\nLe `tanh` sature la vitesse de correction à $v_{y,\\text{max}}$ quelle que soit l'erreur. Si le CM est 2 mètres trop bas, il descend exactement à $v_{y,\\text{max}}$, pas à une vitesse proportionnellement infinie. Ça évite les oscillations et les instabilités numériques.\n\n### La hauteur cible $y_{\\text{tgt}}$ est la somme de plusieurs termes\n\n$$y_{\\text{tgt}} = y_{\\text{sol}} + h_{\\text{nominal}} - \\Delta_{\\text{speed}} - \\Delta_{\\text{slope}} - \\Delta_{\\text{crouch}} + \\Delta_{\\text{jump\\_preload}}$$\n\n- $h_{\\text{nominal}}$ : hauteur nominale calculée par Pythagore depuis la géométrie des jambes\n- $\\Delta_{\\text{speed}}$ : on s'abaisse légèrement à haute vitesse\n- $\\Delta_{\\text{slope}}$ : on s'abaisse en montée (les jambes sont plus inclinées)\n- $\\Delta_{\\text{crouch}}$ : accroupissement contrôlé en descente\n- $\\Delta_{\\text{jump\\_preload}}$ : enfoncement avant l'impulsion de saut\n\nChaque terme correspond à un comportement physique observé chez l'humain.",
  "4": "# Slide 05 — Du CM au bassin\n**Tomas**\n\nMaintenant qu'on sait comment le CM bouge, voyons comment on en déduit le bassin.\n\n### Pourquoi le bassin n'est pas simulé directement ?\n\nOn pourrait simuler le bassin comme une deuxième masse, reliée au CM par une contrainte. Mais ça complexifie le solveur, multiplie l'état, et rend les tests difficiles. Notre choix : le bassin est une **fonction pure du CM**, calculée à chaque frame, sans état propre.\n\n```cpp\nVec2 reconstructPelvis(const CMState& cm,\n                       const CharacterConfig& cfg,\n                       const CharacterReconstructionConfig& rcfg)\n```\n\nCette fonction est déterministe : pour le même `CMState`, elle retourne toujours le même bassin. Pas d'historique, pas d'intégration.\n\n### Ingrédient 1 : décalage vertical fixe\n\nLe bassin est toujours à une distance $d$ sous le CM, où :\n\n$$d = \\text{cm\\_pelvis\\_ratio} \\times L$$\n\nAvec `cm_pelvis_ratio ≈ 0.75` et $L = h_{\\text{corps}} / 5$. Ce ratio est tiré de la proportion humaine : le centre de masse réel d'un humain se situe approximativement à hauteur de nombril, légèrement au-dessus du bassin.\n\n### Ingrédient 2 : lean latéral proportionnel à la vitesse\n\nQuand un humain avance vite, son torse s'incline vers l'avant. On modélise ça avec un angle $\\theta$ qui dépend de $v_x$ :\n\n$$\\theta = \\theta_{\\max} \\cdot \\tanh\\!\\left(\\frac{v_x}{v_{\\text{ref}}}\\right)$$\n\n**Pourquoi `tanh` et pas $\\theta = k \\cdot v_x$ linéaire ?**\n\nAvec un lean linéaire, l'angle croît sans limite avec la vitesse. À $v_x = 10$ m/s, le personnage serait quasiment couché. Le `tanh` sature à $\\theta_{\\max}$ — réponse douce autour de zéro (look naturel à faible vitesse), plafond garanti à haute vitesse. C'est exactement ce que fait un humain réel : l'inclinaison augmente avec la vitesse mais s'asymptote.\n\n### La formule vectorielle complète\n\n$$\\vec{p}_{\\text{bassin}} = \\begin{pmatrix} x_{\\text{cm}} - d\\sin\\theta \\\\ y_{\\text{cm}} - d\\cos\\theta \\end{pmatrix}$$\n\nQuand $\\theta = 0$ : le bassin est exactement à la verticale sous le CM, à distance $d$.\nQuand $\\theta > 0$ (avance vers la droite) : le bassin est légèrement décalé vers la gauche et vers le haut — ce qui crée le lean vers l'avant naturellement.\n\n### Interprétation géométrique\n\nImaginez un vecteur de longueur $d$ partant du CM, initialement pointant vers le bas. L'angle $\\theta$ fait pivoter ce vecteur. L'extrémité de ce vecteur, c'est le bassin. Simple, robuste, aucune instabilité possible.",
  "5": "# Slide 06 — Cinématique inverse des jambes\n**Tomas**\n\nOn a le bassin. On a le pied (son état est géré par les triggers de pas et mis à jour à chaque frame). Il reste à trouver le **genou**.\n\n### Le problème IK à deux segments\n\nDonnées : $P$ (bassin), $F$ (pied), $L$ (longueur d'un segment — cuisse = tibia = $L$).\nCherché : $K$ (genou).\n\nC'est un problème de cinématique inverse classique. Il a en général **deux solutions** (genou devant ou derrière), et on choisit toujours celle où le genou est du côté du mouvement.\n\n### Pourquoi analytique plutôt qu'itératif ?\n\nOn aurait pu utiliser un algorithme FABRIK, ou Newton-Raphson sur les contraintes. Ces méthodes fonctionnent bien pour des chaînes longues et des structures complexes. Mais pour **deux segments de longueur égale**, la solution est un simple triangle isocèle — aucune itération nécessaire.\n\nL'analytique est préférable ici parce que :\n- **Résultat exact et déterministe** à chaque frame\n- **Pas de convergence** à attendre (pas de problème si $dt$ est irrégulier)\n- **Facile à tester** : étant donné $P$, $F$, $L$, on vérifie que $|KP| = |KF| = L$\n\n### La dérivation géométrique\n\nLes deux segments $PK$ et $KF$ ont la même longueur $L$. $P$, $K$, $F$ forment donc un **triangle isocèle**. La hauteur $h_k$ de ce triangle, issue de $K$ vers la base $PF$, est donnée par Pythagore :\n\n$$h_k = \\sqrt{L^2 - \\left(\\frac{d}{2}\\right)^2}, \\quad \\text{où } d = \\|P - F\\|$$\n\nLe genou se place au milieu $M = (P+F)/2$, déplacé de $h_k$ sur la perpendiculaire à $PF$ :\n\n$$K = M + h_k \\cdot \\hat{n}_{\\perp}$$\n\nLa perpendiculaire $\\hat{n}_{\\perp}$ est obtenue par rotation CCW de $\\hat{e}_{PF}$ : si $\\hat{e} = (e_x, e_y)$, alors $\\hat{n} = (-e_y, e_x)$. On inverse le signe si $n_x \\cdot \\text{facing} < 0$ pour forcer le genou devant.\n\n### Gestion des singularités\n\n**Extension totale** ($d > 2L - \\varepsilon$) : on clamp le pied dans le disque de portée avant de calculer. Sans ça, $\\sqrt{L^2 - (d/2)^2}$ serait $\\sqrt{\\text{négatif}}$ → NaN.\n\n**Pied sous le bassin** ($d < \\varepsilon$) : on retourne un genou par défaut vers l'avant. Sans ça, division par zéro dans la normalisation de $\\hat{e}_{PF}$.\n\nCes deux cas sont vérifiés dans les scénarios headless (`no NaN in cm_x` vérifie indirectement que l'IK ne produit pas de valeurs invalides).\n\n### Différence avec les bras\n\nPour les bras, `upper_arm ≠ forearm`. Le triangle n'est plus isocèle. On utilise la formule générale :\n$$a = \\frac{u^2 - f^2 + d^2}{2d}, \\quad h = \\sqrt{u^2 - a^2}$$\noù $u$ = longueur du bras supérieur, $f$ = longueur de l'avant-bras. Le coude est placé avec une préférence de pliure et une stabilité temporelle (on évite que le coude ne saute d'un côté à l'autre).",
  "6": "# Slide 07 — XCoM et marge de stabilité\n**Tomas**\n\nOn arrive au cœur du modèle de stabilité. C'est la partie la plus directement ancrée dans la littérature scientifique.\n\n### La référence : Hof (2008)\n\n*The extrapolated center of mass concept suggests a simple control of balance in walking.* Human Movement Science, 27(1), 112–125.\n\nHof montre expérimentalement sur des humains marchant sur un tapis roulant que la condition nécessaire et suffisante de stabilité d'une marche bipède peut s'exprimer avec un seul scalaire : le **XCoM** ou centre de masse extrapolé, noté $\\xi$.\n\n### Le modèle du pendule inversé\n\nL'idée est de modéliser le CM comme la masse d'un **pendule inversé** à jambes télescopiques. Dans ce modèle (Kajita et al., 2001), si le CM est à hauteur $h$ au-dessus du sol et que personne ne lui applique de force, il va tomber. La fréquence propre de cette chute est :\n\n$$\\omega_0 = \\sqrt{\\frac{g}{h_{\\text{ref}}}}\n$$\n\nPar exemple, pour $h_{\\text{ref}} = 1$ m et $g = 9.81$ m/s² : $\\omega_0 \\approx 3.13$ rad/s. C'est la rapidité avec laquelle le CM accélèrerait s'il n'y avait aucun appui.\n\n### Le XCoM : position prédite sans intervention\n\nSi on laisse le CM avec sa vitesse actuelle $v_x$ sans appliquer de force, il atterrira approximativement à :\n\n$$\\xi = x_{\\text{cm}} + \\frac{v_x}{\\omega_0}$$\n\nPlus $v_x$ est grand, plus $\\xi$ est loin devant. Plus $h$ est petit (jambes fléchies), plus $\\omega_0$ est grand et plus $\\xi$ se rapproche du CM — on a moins besoin d'anticiper.\n\n### La marge de stabilité (MoS)\n\nLe polygone de support est l'intervalle $[x_L, x_R]$ des deux pieds. La **Margin of Stability** est :\n\n$$\\text{MoS} = \\min(\\xi - x_L,\\ x_R - \\xi)$$\n\n- $\\text{MoS} > 0$ : le CM peut théoriquement s'arrêter seul au-dessus du support.\n- $\\text{MoS} < 0$ : le XCoM est sorti du support — le CM va tomber si on ne fait rien.\n\n### Pourquoi ne pas utiliser seulement $x_{\\text{cm}}$ ?\n\nImaginez un personnage qui court vers la droite à 3 m/s. Son CM est encore à $x = 0$, entre les deux pieds. Par position seule, il semble stable. Mais dans 300 ms, son CM sera à $x = 0.9$ m — peut-être déjà sorti du support. Le XCoM anticipe cette dynamique en intégrant vitesse et position en un seul scalaire.",
  "7": "# Slide 08 — Deux déclencheurs de pas\n**Tomas**\n\nSavoir que $\\text{MoS} < 0$ signifie qu'il faut faire un pas. Mais il y a une deuxième raison, indépendante, de faire un pas : la cadence. Ce slide explique pourquoi il faut **deux triggers séparés**.\n\n### Trigger 1 — Cadence (rear trigger)\n\nMême si le CM est parfaitement stable, si le pied arrière est trop loin du bassin, la prochaine oscillation du CM va sortir du support. On anticipe ça avec un trigger de cadence :\n\n```cpp\neval.d_rear = (pelvis.x - eval.rear_x) * facing;  // distance pied arrière → bassin\neval.rear_trigger = eval.d_rear > d_rear_max * L;  // trop loin ?\n```\n\nCe trigger règle le **rythme** de la marche. Sans lui, le personnage ne ferait jamais de pas pendant une marche lente sur terrain plat (MoS toujours positif) et s'arrêterait.\n\n### Trigger 2 — Urgence (xcom trigger)\n\nSi $\\xi$ dépasse le pied avant dans la direction de marche, c'est une urgence :\n\n```cpp\neval.xcom_trigger = std::abs(velocity_x) > eps_step\n                 && (xi - eval.front_x) * facing > 0.0;\n```\n\nLa condition `|v_x| > eps` évite les déclenchements intempestifs quand le personnage est presque arrêté (le XCoM oscille légèrement mais ne sort pas vraiment).\n\n### Pourquoi ne pas fusionner les deux ?\n\nC'est une question naturelle. Si on fusionnait en `trigger = xcom_trigger || rear_trigger`, on perdrait la capacité à les régler indépendamment :\n- Si on rend la condition trop sensible → trop de pas de récupération pendant une marche normale, look saccadé\n- Si on la rend trop paresseuse → pas de réaction d'urgence assez rapide, personnage qui tombe\n\nLes deux problèmes (cadence et récupération) ont des échelles de temps et des importances différentes.\n\n### La cible du pas — placer le pied pour stabiliser\n\nUne fois le trigger activé, où pose-t-on le pied ? En marche :\n\n$$x_{\\text{target}} = \\xi + f \\cdot m_{\\text{stab}} \\cdot L$$\n\noù $f$ est la direction de marche et $m_{\\text{stab}}$ une marge de stabilité. On pose le pied **devant** $\\xi$ pour que le nouveau support contienne $\\xi$ avec une marge positive.\n\nSur pente, la portée horizontale effective est réduite par la géométrie (une partie de la longueur de jambe est consommée verticalement) :\n\n$$x_{\\text{max}} = 0.95 \\cdot r_{\\text{portée}} / \\sqrt{1 + \\text{slope}^2}$$\n\nCette limite évite de demander à l'IK une cible impossible à atteindre.",
  "8": "# Slide 09 — Référence de sol stable\n**Tomas**\n\nOn a maintenant un CM qui se déplace et des triggers qui déclenchent des pas. Mais il reste un problème : sur terrain irrégulier, **quelle hauteur de sol utiliser comme référence** pour le setpoint vertical du CM ?\n\n### Trois approches possibles\n\n**Approche naïve 1 — hauteur locale sous le CM** : le CM tremblerait à chaque bosse. Chaque changement de pente déclencherait une correction verticale brusque.\n\n**Approche naïve 2 — hauteur fixe** : les pieds flotteraient dans l'air en montée, le CM serait trop bas en descente.\n\n**Notre approche — fenêtre glissante lissée** : on échantillonne le terrain en deux points de part et d'autre du bassin, on moyenne, et on lisse temporellement.\n\n### La fenêtre d'observation\n\n$$x_{\\text{back}} = x_{\\text{cm}} - f \\cdot w_{\\text{back}} \\cdot L$$\n$$x_{\\text{front}} = x_{\\text{cm}} + f \\cdot \\left(w_{\\text{fwd}} \\cdot L + |v_x| \\cdot t_{\\text{look}}\\right)$$\n\nLe terme $|v_x| \\cdot t_{\\text{look}}$ est crucial : c'est un **lookahead proportionnel à la vitesse**. En marche lente, la fenêtre est petite — on regarde juste sous les pieds. En course, on regarde plus loin devant pour anticiper les changements de terrain. C'est un comportement humain : quand on court, le regard va loin devant.\n\n### Lissage exponentiel — et pas filtre passe-bas classique\n\nLes extrémités de la fenêtre glissent vers leur cible avec :\n\n$$\\alpha = 1 - e^{-\\Delta t / \\tau}$$\n\n**Pourquoi pas un filtre passe-bas discret classique** $y_n = (1-\\alpha)y_{n-1} + \\alpha x_n$ avec $\\alpha$ constant ? Parce qu'avec un $\\alpha$ fixe, le comportement dépend du $\\Delta t$. Si un frame dure deux fois plus longtemps, le filtre est deux fois trop lent. La forme $1 - e^{-\\Delta t / \\tau}$ **donne le même comportement indépendamment du framerate** — c'est stable même si le jeu rame.\n\n### Pente et hauteur de référence\n\n$$\\text{ref\\_ground} = \\frac{y_{\\text{back}} + y_{\\text{front}}}{2}, \\quad \\text{slope} = \\frac{y_{\\text{front}} - y_{\\text{back}}}{|x_{\\text{front}} - x_{\\text{back}}|}$$\n\nLa pente est utilisée pour adapter $y_{\\text{tgt}}$ (abaissement en montée, accroupissement en descente) et pour corriger la portée horizontale des jambes.\n\n### L'algorithme de clamp par bisection\n\nAvant de glisser, on vérifie que le point cible reste dans le **disque de portée du bassin** (rayon $\\approx 2L$). Si la pente est très forte, un point trop loin peut être hors de portée des jambes. On cherche le dernier point valide par bisection à 28 itérations :\n\n```cpp\nfor (int j = 0; j < 28; ++j) {\n    const double xm = 0.5 * (x_valid + x_invalid);\n    if (inside(xm)) x_valid = xm;\n    else            x_invalid = xm;\n}\n```\n\nPrécision de $2^{-28}$ sur la position, sans solveur coûteux — c'est un exemple d'algorithme simple mais exact.",
  "9": "# Slide 10 — Rendre la physique lisible\n**Meleik**\n\nMaintenant on a toute la physique : CM intégré, bassin reconstruit, genoux calculés par IK, pieds placés par les triggers. Il reste à transformer tout ça en pixels.\n\n### Architecture du renderer — passif par conception\n\nLe `CharacterRenderer` reçoit un `CharacterState` et le transforme en pixels SDL2. Il ne prend aucune décision physique. Le pipeline est :\n\n```\nSimulationCore::step()  →  CharacterState  →  CharacterRenderer  →  SDL2\n```\n\nCette séparation est importante : si le rendu était entremêlé avec la physique, on ne pourrait pas exécuter le headless. Et si le renderer modifiait les positions, les overlays ne correspondrait plus aux valeurs réelles.\n\n### Deux modes de rendu sur le même `CharacterState`\n\n**Mode legacy** : lignes droites bassin → genou → pied. Visuellement simple, mais montre exactement les articulations calculées. Utilisé pour vérifier que l'IK est correcte.\n\n**Mode spline** : courbes de Bézier cubiques entre les mêmes points. Plus lisible pour une démo ou une présentation. Les deux partent du **même** `CharacterState` — le renderer ne triche pas.\n\n### Construction des courbes de Bézier pour une jambe\n\nÉtant donné $P_0$ (bassin), $K$ (genou), $P_3$ (pied), les points de contrôle internes sont placés sur les segments de la jambe :\n\n$$P_1 = P_0 + \\frac{2}{3}(K - P_0), \\quad P_2 = P_3 - \\frac{2}{3}(P_3 - K)$$\n\nLa courbe de Bézier cubique est :\n$$B(t) = (1-t)^3 P_0 + 3(1-t)^2 t P_1 + 3(1-t)t^2 P_2 + t^3 P_3, \\quad t \\in [0,1]$$\n\nEn plaçant les handles aux $2/3$ des segments genou → bassin et genou → pied, la courbe reste très proche du genou au milieu ($t \\approx 0.5$) tout en ayant des tangentes lisses aux extrémités.\n\n### Ordre de profondeur selon l'orientation\n\n```cpp\nconst bool facing_right = character.facing >= 0.0;\nconst Vec2& back_knee   = facing_right ? character.knee_right : character.knee_left;\nconst Vec2& front_knee  = facing_right ? character.knee_left  : character.knee_right;\n```\n\nOn dessine les membres arrière avant les membres avant. L'ordre change selon la direction du personnage — un détail visuel important pour la lisibilité.\n\n### La tête : CIRCLE_KAPPA\n\n```cpp\nstatic constexpr double CIRCLE_KAPPA = 0.5522847498307936;\n```\n\nCette constante permet d'approximer un cercle parfait avec **quatre courbes de Bézier cubiques**. L'erreur est inférieure à 0.03 %. C'est une constante classique de géométrie computationnelle, pas un nombre magique — elle vient de la condition que les tangentes de la Bézier correspondent à la tangente du cercle aux points de jointure.",
  "10": "# Slide 11 — Validation headless\n**Gedik**\n\nUne marche peut paraître correcte à l'œil tout en ayant une régression numérique silencieuse. On a donc construit un système de validation automatique sans interface graphique.\n\n### Principe : mêmes règles, pas de fenêtre\n\nLe binaire headless compile **sans SDL, sans ImGui, sans `src/render`**. Il exécute le même `SimulationCore::step()` avec des entrées **déterministes** :\n\n```cpp\ndef.input_fn = [](double t) -> InputFrame {\n    InputFrame f;\n    f.key_right = (t < 1.5);   // marche vers la droite pendant 1.5s\n    return f;\n};\n```\n\nAvec le même seed de terrain et le même $\\Delta t$ fixe, le résultat est **bit-pour-bit reproductible**. Pas d'aléatoire, pas de dépendance au framerate réel.\n\n### 11 scénarios couvrant 5 familles\n\n| Famille | Scénarios |\n|---|---|\n| Stabilité statique | `stand_still` — le personnage reste debout sans input |\n| Marche | `walk_3s`, `walk_then_stop`, `fast_walk`, `walk_max_from_start`, `walk_left` |\n| Course | `run_3s` — entre en état `Running`, produit du support simple |\n| Perturbation | `perturbation_recovery` — vitesse forcée vers l'arrière à $t=0.5$ s |\n| Saut | `jump_from_stand`, `jump_from_walk` |\n\nLe scénario `perturbation_recovery` est particulièrement intéressant : il injecte `set_velocity = {-2.0, 0.0}` pendant 20 ms pendant que le personnage marche. Ça simule un choc. On vérifie qu'il ne reste pas en état aérien et ne produit pas de NaN.\n\n### Types d'assertions\n\n```cpp\n// Assertion quantitative : le personnage a avancé\nrec.addAssertion(\"cm_x > 1.5 at end\", [](const auto& rows) {\n    return !rows.empty() && rows.back().cm_x > 1.5;\n});\n// Assertion d'état : il a bien marché\nrec.addAssertion(\"loco_state Walking at some point\", [](const auto& rows) {\n    return std::any_of(rows.begin(), rows.end(),\n        [](const auto& r) { return r.loco_state == LocomotionState::Walking; });\n});\n// Assertion numérique : pas de NaN\nrec.addAssertion(\"no NaN in cm_x\", [](const auto& rows) {\n    return std::none_of(rows.begin(), rows.end(),\n        [](const auto& r) { return r.cm_x != r.cm_x; });  // NaN != NaN\n});\n```\n\n### Gestion mémoire — Valgrind et sanitizers\n\n```bash\nmake test_mem   # Valgrind --leak-check=full sur les 11 scénarios\nmake test_asan  # AddressSanitizer + UBSanitizer\n```\n\nLe projet évite les allocations manuelles : objets par valeur, `std::vector`, `std::optional`, références constantes. Aucun pointeur nu. Valgrind est lancé sur le binaire headless entier — il exécute les 11 scénarios complets, soit environ 33 secondes de simulation physique.\n\n### Télémétrie CSV\n\nChaque scénario produit un CSV avec colonnes `t`, `cm_x`, `cm_y`, `vx`, `vy`, `loco_state`, `left_pinned`, `right_pinned`... Ce CSV peut être ouvert dans un tableur ou analysé avec Python pour visualiser la trajectoire — pratique pour déboguer un scénario qui échoue.",
  "11": "# Slide 12 — Conclusion\n**Tomas, puis Gedik**\n\n### Ce qu'on a construit — Tomas\n\nBobTricks démontre qu'une locomotion procédurale lisible et testable est atteignable avec un modèle physique compact. Le pipeline complet tient dans quelques centaines de lignes de code clair :\n\n1. **Euler semi-implicite** intègre le CM avec un attracteur tanh pour le vertical\n2. **`reconstructPelvis`** dérive le bassin avec lean $\\theta_{\\max}\\tanh(v_x/v_{\\text{ref}})$\n3. **`computeKnee`** résout l'IK analytiquement avec $h_k = \\sqrt{L^2 - (d/2)^2}$\n4. **XCoM** (Hof, 2008) et **MoS** décident quand poser un pied\n5. **Deux triggers** séparés maintiennent cadence ET récupèrent l'équilibre\n6. **Référence de sol** lissée évite les tremblements sur terrain irrégulier\n7. **Bézier** sur les trois points physiques rend la pose lisible à l'écran\n\nLe tout est **fondé scientifiquement** : XCoM de Hof (2008), pendule inversé linéaire de Kajita (2001). Ce ne sont pas des formules inventées — ce sont des modèles validés en robotique et biomécanique.\n\n### Ce qu'on a livré — Gedik\n\n- **Makefile complet** : `build`, `run`, `test`, `test_mem`, `test_asan`, `docs` — tout en une commande\n- **11 scénarios headless** avec assertions quantitatives et CSV de télémétrie\n- **Doxygen** générable avec `make docs`\n- **README** comme manuel d'utilisation complet\n- **Documentation** : mathématiques, décisions d'architecture, guide de tuning, UML\n- **Linux + Windows** : exécutable multiplateforme\n\n### Limites assumées\n\n- Modèle **2D stylisé** — pas un jumeau numérique biomécanique\n- Pas de mécanique de contact complète (frottements, moments angulaires)\n- Validation encore **qualitative** : on vérifie que ça marche et ne plante pas, pas qu'on reproduit des données gaitlab\n\n### Questions probables du jury\n\n**Pourquoi pas des animations enregistrées ?**\nUne animation fixe ne réagit pas à la vitesse réelle, au terrain, à un déséquilibre. Ici, le placement des pieds dépend de l'état physique à chaque frame.\n\n**Pourquoi pas un solveur d'articulations complet ?**\nTrop instable et complexe à régler sur 3 mois. Notre compromis — simuler le CM et reconstruire le reste — est suffisamment riche pour produire une marche lisible et testable.\n\n**Pourquoi deux triggers de pas ?**\nCadence $\\neq$ récupération d'équilibre. Ce sont deux problèmes avec des échelles de temps différentes. Fusionner les deux dégrade les deux.\n\n**Comment prouvez-vous que ça marche ?**\nTrois niveaux : visuel (rendu + overlays), quantitatif (assertions headless), numérique (Valgrind + ASan — zéro fuite mémoire, zéro comportement indéfini)."
};
